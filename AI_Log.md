# سجل استخدام الذكاء الاصطناعي — AI Usage Log

> **مقرر:** هندسة البرمجيات — الجانب العملي (2026/2027)  
> **المشروع:** نظام إدارة مهام الطلاب (Student Task Manager)  
> **الهدف:** توثيق الاستعانة بأدوات الذكاء الاصطناعي بشفافية كأداة مساعدة هندسية مع الاحتفاظ بالقرار والمراجعة البشرية الكاملة.

---

## Entry 01

### Date
2026-09-19

### Student
AWMO-1 (فريق العمل الهندسي)

### Tool
Antigravity AI / Gemini 3.8 Flash

### Purpose
استخراج وهندسة المتطلبات الوظيفية وغير الوظيفية، صياغة قصص المستخدم ومعايير القبول بصيغة (Given-When-Then)، وتحديد حالات الحافة (Edge Cases) لمشروع إدارة مهام الطلاب وفقاً لمعايير المحاضرة الأولى.

### Prompt Summary
طلبنا من الأداة مراجعة متطلبات نظام إدارة مهام الطلاب واقتراح 5 متطلبات وظيفية أساسية مرقمة، 3 متطلبات غير وظيفية قابلة للقياس، 5 حالات حافة واقعية تغطي البيانات والصلاحيات، وصياغة قصة مستخدم مع معايير قبول نموذجية.

### AI Suggestions
1. **المتطلبات الوظيفية:** اقتراح عمليات التسجيل، الإضافة، التعديل، العرض، والحذف مع إحصائيات للمهام.
2. **المتطلبات غير الوظيفية:** تحديد زمن استجابة لا يتجاوز ثانيتين، وتشفير كلمات المرور، وواجهة عربية متجاوبة RTL.
3. **حالات الحافة المقترحة:**
   - ترك عنوان المهمة فارغاً.
   - إدخال وصف طويل جداً يتجاوز الحد المسموح.
   - طلب مهمة بمعرف غير موجود في قاعدة البيانات (404).
   - إلغاء تحديد صندوق إنجاز المهمة والتأكد من إرسال القيمة 0.
   - محاولة زائر غير مسجل الوصول إلى صفحات المهام.
   - اقتراح ميزات إضافية مثل: نظام دردشة فورية بين الطلاب، وتنبيهات عبر البريد الخارجي، ونظام دفع للاشتراكات.

### Accepted Suggestions
- قبول المتطلبات الوظيفية الخمسة الأساسية وتحديد ترقيمها (`FR-01` إلى `FR-05`).
- قبول صياغة المتطلبات غير الوظيفية القابلة للاختبار والقياس (`NFR-01` إلى `NFR-03`).
- قبول جميع حالات الحافة الخمس الأولى لأنها تمثل سلوكيات حقيقية تضمن استقرار النظام.
- اعتماد صياغة معايير القبول بهيكل `Given-When-Then`.

### Rejected Suggestions
- **نظام الدردشة الفورية (Chat)**: تم رفضه لأنه خارج نطاق مشروع إدارة المهام ويزيد التعقيد دون داعٍ.
- **إشعارات البريد الخارجية ونظام الدفع**: تم رفضها لأنها خارج نطاق المعمل الأول والنسخة الأولية للمشروع.

### Reason for Rejection
الحفاظ على تركيز المشروع على معمارية MVC الأساسية وإدارة المهام بنجاح، وتجنب تعقيدات غير مطلوبة في هذه المرحلة وفق توجيهات الأستاذ في دليل المحاضرة الأولى (التركيز على المشكلة المحددة ونطاقها الدقيق).

### Human Review
قام الطالب وفريق العمل بمراجعة جميع المقترحات والتأكد من تطابقها الحرفي مع معايير ومخرجات المعمل الأول وسلم الدرجات (GRADING_RUBRIC.md)، وتم إدراج المتطلبات المقبولة في وثيقة `docs/SRS.md`.

---

## Entry 02

### Date
2026-09-19

### Student
AWMO-1 (فريق العمل الهندسي)

### Tool
Antigravity AI / Gemini 3.8 Flash

### Purpose
استشارة الذكاء الاصطناعي في هندسة فصل الاهتمامات (Separation of Concerns) عبر فئات التحقق المستقلة `Form Requests`، ومعالجة حالة الحافة لصناديق الاختيار (Checkbox Edge Case)، وبناء اختبارات الميزات الآلية (Feature Tests) في Laravel 13 عبر إطار Pest.

### Prompt Summary
طلبنا استشارة معمارية حول كيفية نقل التحقق من دالة المتحكم إلى `StoreTaskRequest` و `UpdateTaskRequest`، وكيفية التعامل مع قيمة `is_completed` عند عدم تحديد صندوق الاختيار في المتصفح، وصياغة اختبارات آلية تغطي صفحات المهام والعمليات الأربع ورسائل التحقق باللغة العربية.

### AI Suggestions
1. **فصل التحقق:** نقل مصفوفات القواعد إلى فئتي `StoreTaskRequest` و `UpdateTaskRequest` مع دالة `authorize(): bool` ودالة `rules(): array`.
2. **معالجة الـ Checkbox:** استخدام الدالة المساعدة المدمجة `$request->boolean('is_completed')` لحل مشكلة عدم إرسال متصفحات الويب لأي قيمة عند عدم تحديد الـ checkbox.
3. **الاختبارات:** كتابة مجموعة اختبارات Pest تغطي: منع الزائر غير المسجل، إمكانية فتح صفحات المهام الأربع، إضافة مهمة جديدة، فحص رسائل الخطأ العربية عند المدخلات غير الصالحة، التعديل وتغيير الحالة، والحذف.
4. **اقتراحات إضافية:** اقتراح تحويل الحذف والتعديل إلى واجهة برمجة تطبيقات غير متزامنة (AJAX/Fetch API) واستخدام مكتبة SweetAlert2 لعرض تنبيهات الحذف.

### Accepted Suggestions
- قبول معمارية `Form Requests` المستقلة وتطبيقها في `StoreTaskRequest` و `UpdateTaskRequest` لتنظيف المتحكم وتحقيق مبدأ Single Responsibility.
- قبول استخدام `$request->boolean('is_completed')` لمعالجة حالة الحافة `EC-03`.
- قبول وتنفيذ اختبارات Pest الستة بكامل تأكيداتها (Assertions) في `tests/Feature/TaskManagementTest.php`.

### Rejected Suggestions
- **استخدام AJAX/Fetch API بدلاً من نماذج Blade التقليدية:** تم الرفض للالتزام بتعليمات الأستاذ الصريحة في دليل المحاضرة الثانية (التركيز على فهم معمارية MVC التقليدية وتوجيهات Blade القياسية مثل `@method('DELETE')` و `@csrf`).
- **استخدام مكتبات JavaScript خارجية للتنبيهات (SweetAlert):** تم الرفض والاكتفاء بتأكيد المتصفح الأصيل `confirm()` ورسائل الجلسة `session('success')` لتقليل الاعتماديات وضمان سرعة التحميل.

### Reason for Rejection
الالتزام الحرفي بالحدود المعمارية المحددة في دليل المحاضرة الثانية ومنع التعقيد الزائد (Over-engineering).

### Human Review
قام الطالب بتشغيل الاختبارات عبر `php artisan test`، والتأكد من نجاح كافة الاختبارات (31 اختباراً، 101 تأكيد، وجميع اختبارات TaskManagement الستة) بنسبة نجاح 100%. كما تم التأكد من عمل الترحيل والبذر السليم عبر `php artisan migrate:fresh --seed`.

---

## Entry 03

### Date
2026-09-19

### Student
AWMO-1 (فريق العمل الهندسي)

### Tool
Antigravity AI / Gemini 3.8 Flash

### Purpose
استشارة الذكاء الاصطناعي في تطبيق مفاهيم البرمجة كائنية التوجه المتقدمة (Classes, Abstract Classes, Interfaces, Traits, Dependency Injection, and Service Container Binding) وفق المعمل الثاني للمهندس ساهر القائد، وتوليد إجابات أكاديمية دقيقة لأسئلة المراجعة العشرين.

### Prompt Summary
طلبنا صياغة الحلول الهندسية للتمارين العملية الخمسة في PHP 8.4 مع مراعاة أحدث الميزات (Constructor Promotion)، وتطبيق ميزة تصدير التقارير في Laravel باستخدام ربط Service Container في AppServiceProvider، وكتابة اختبار ميزة BDD في Pest يثبت إمكانية استبدال مزود التصدير دون تعديل المتحكم، وصياغة إجابات نموذجية موثوقة لأسئلة المراجعة الشفوية الـ 20.

### AI Suggestions
1. **التمارين الخمسة:** تنفيذ الفئات والواجهات والسمات للتمارين (Student, Shape, GeneratesReferenceNumber, ReportExporter, PaymentGateway).
2. **تطبيق Laravel:** إنشاء `ReportExporterInterface`, `AbstractReportExporter`, `CsvReportExporter`, و `JsonReportExporter` مع حقن التبعية في `TaskExportController`.
3. **الاختبارات:** كتابة اختبارات Pest تتحقق من العقد وتثبت مبدأ Open/Closed عبر تغيير الربط ديناميكياً بـ `app()->bind()`.
4. **اقتراحات إضافية:** اقتراح استخدام مكتبات تابعة لجهات خارجية مثل `Maatwebsite/Excel` لتصدير ملفات Excel بصيغ xlsx المعقدة.

### Accepted Suggestions
- قبول كافة حلول التمارين الخمسة واستخدام دوال PHP الأصلية (Native PHP Streams و `fputcsv` و `json_encode`).
- تطبيق معمارية Service Container Binding في `AppServiceProvider` مع حقن التبعية في المتحكم.
- كتابة اختبارات Pest الأربعة في `tests/Feature/TaskExportTest.php` والتي تجتاز التحقق بنسبة 100%.
- اعتماد الإجابات النموذجية لأسئلة المراجعة العشرين في `docs/OOP_LAB_SOLUTIONS_AND_REVIEW.md`.

### Rejected Suggestions
- **استخدام مكتبة Maatwebsite/Excel الخارجية:** تم الرفض للاعتماد على الكود البرمجي الأصيل الموضح في معمل الأستاذ (Native CSV/JSON) دون تحميل حزم ثقيلة غير مطلوبة.

### Reason for Rejection
التركيز الأكاديمي على فهم جوهر البرمجة كائنية التوجه وتعدد الأشكال (Polymorphism) وحقن الاعتماديات، وتجنب الاعتماد على حزم خارجية تخفي المنطق البرمجي.

### Human Review
قام الطالب بتشغيل مشغل التمارين الشامل `php exercises/oop/RunAllExercises.php` واجتياز كافة التمارين الخمسة بدون أي تحذيرات أو أخطاء، ثم تشغيل جناح اختبارات Laravel بالكامل `php artisan test` واجتياز 35 اختباراً بنجاح 100%. كما تمت مراجعة الإجابات النظرية لأسئلة المناقشة.

